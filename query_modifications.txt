# TPC-H Query 1, as generated
select
	l_returnflag,
	l_linestatus,
	sum(l_quantity) as sum_qty,
	sum(l_extendedprice) as sum_base_price,
	sum(l_extendedprice * (1 - l_discount)) as sum_disc_price,
	sum(l_extendedprice * (1 - l_discount) * (1 + l_tax)) as sum_charge,
	avg(l_quantity) as avg_qty,
	avg(l_extendedprice) as avg_price,
	avg(l_discount) as avg_disc,
	count(*) as count_order
from
	lineitem
where
	l_shipdate <= date '1998-12-01' - interval '61' day (3)
group by
	l_returnflag,
	l_linestatus
order by
	l_returnflag,
	l_linestatus;
set rowcount -1
go

# TPC-H Query 1, BigQuery syntax


# TPC-H Query 5, as generated by qgen
select
	n_name,
	sum(l_extendedprice * (1 - l_discount)) as revenue
from
	customer,
	orders,
	lineitem,
	supplier,
	nation,
	region
where
	c_custkey = o_custkey
	and l_orderkey = o_orderkey
	and l_suppkey = s_suppkey
	and c_nationkey = s_nationkey
	and s_nationkey = n_nationkey
	and n_regionkey = r_regionkey
	and r_name = 'AFRICA'
	and o_orderdate >= date '1997-01-01'
	and o_orderdate < date '1997-01-01' + interval '1' year
group by
	n_name
order by
	revenue desc;
set rowcount -1
go


# TPC-H Query 5, BigQuery syntax

# q5
select
	n_name,
	sum(l_extendedprice * (1 - l_discount)) as revenue
from
	h_1GB_1_basic.CUSTOMER,
	h_1GB_1_basic.ORDERS,
	h_1GB_1_basic.LINEITEM,
	h_1GB_1_basic.SUPPLIER,
	h_1GB_1_basic.NATION,
	h_1GB_1_basic.REGION
where
	c_custkey = o_custkey
	and l_orderkey = o_orderkey
	and l_suppkey = s_suppkey
	and c_nationkey = s_nationkey
	and s_nationkey = n_nationkey
	and n_regionkey = r_regionkey
	and r_name = 'ASIA'
	and o_orderdate >= DATE '1994-01-01'
#	and o_orderdate < date '1997-01-01' + interval '1' year
  and o_orderdate < date_add(DATE '1994-01-01', interval '1' year)
group by
	n_name
order by
	revenue desc;
#set rowcount -1
#go